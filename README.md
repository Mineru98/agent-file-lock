# agent-file-lock (`afl`)

Lock files that coding agents must never modify, using OS-level immutable flags
(Linux `FS_IMMUTABLE_FL`, macOS `SF_IMMUTABLE`). Single static Go binary, no
runtime dependencies.

Work in progress.
